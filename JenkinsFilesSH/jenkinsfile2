pipeline {

    agent any
    environment{
        name= "rahul"
    }
    stages{
         stage('Print'){
            steps{
                sh '''
                    echo "hello ${name}"
                '''
            }
        }
    }
}